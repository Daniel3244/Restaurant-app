spring.application.name=restaurant-backend
server.port=8081
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

app.jwt.secret=${APP_JWT_SECRET:change-me-in-prod}
app.jwt.ttl-hours=${APP_JWT_TTL_HOURS:8}

